# 0.2.2 用户权限与网盘额度修复

- 修复打开“修改用户”时私人网盘额度偶尔为空的问题。
- 修改框每次打开都会从服务器重新读取最新额度和全部权限状态。
- 修复不完整的权限更新请求会把未提交权限重置为关闭的问题。
- 围棋权限固定显示在管理页面 HTML 中，不再依赖异步脚本临时添加。
- 保存后自动重新读取用户状态；若服务器状态与提交内容不一致，会提示错误而不是静默关闭权限。
- 管理员账号的权限显示为全部开启且不可单独关闭，符合后端的实际授权规则。
- 新增权限回归测试，覆盖全部 21 个权限字段、旧用户兼容及 0 MB 网盘额度。
