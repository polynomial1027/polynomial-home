{
  "name": "polynomial-server-home",
  "version": "4.0.0",
  "private": true,
  "description": "Polynomial Server portal with courses, isolated Python labs and randomized assignment grading",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/store.js && node --check lib/python-course.js && node --check public/assets/site.js && node --check public/assets/learning.js"
  },
  "dependencies": {
    "busboy": "^1.6.0",
    "http-proxy": "^1.18.1",
    "ws": "^8.18.3"
  },
  "engines": {
    "node": ">=20"
  }
}
